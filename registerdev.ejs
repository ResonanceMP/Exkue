<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Elixir</title>
    <link rel="icon" href="https://cdn.itztiva.com/elixirlogo.png">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:wght@400;700&display=swap">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: url(https://cdn.itztiva.com/websitebackground.png) center/cover no-repeat;
            font-family: 'Bricolage Grotesque', sans-serif;
            overflow: hidden;
            position: relative;
        }

        .container {
            position: fixed;
            top: 0;
            bottom: 0;
            left: 0;
            width: 500px;
            text-align: center;
            z-index: 1;
            background-color: rgba(0, 0, 0, 0.815);
            padding: 85px;
            border-radius: 10px;
            box-shadow: 0px 0px 10px 0px rgba(0, 0, 0, 0.75);
        }

        h1 {
            margin-bottom: 30px;
            color: #fff;
            font-size: 24px;
        }

        b {
            margin-top: 10px;
            margin-bottom: 10px;
            color: #fff;
            font-size: 16px;
        }

        .container a {
            display: block;
            margin-bottom: 20px;
            padding: 15px 0;
            border-radius: 10px;
            color: #fff;
            background: #5865f2;
            text-decoration: none;
            transition: background 0.3s ease;
        }

        .container a:hover {
            background: #7289da;
        }

        img.logo {
            width: 100px;
            height: 100px;
            margin-bottom: 20px;
        }

        img.discord-logo {
            height: 23px;
            vertical-align: middle;
            margin-left: 5px;
        }

        .auth-box {
            position: relative;
            background-color: rgb(31, 31, 31);
            padding: 30px;
            border-radius: 10px;
            margin-bottom: 20px;
        }

        form {
            margin-top: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            position: relative;
        }

        label {
            color: #fff;
            margin-bottom: 5px;
        }

        input {
            padding: 13px;
            margin-bottom: 10px;
            border-radius: 5px;
            border: 1px solid #050505;
            width: 100%;
            max-width: 300px;
            background-color: #2e2e2e;
            color: #fff;
        }

        button {
            padding: 10px 64px;
            background-color: #7289DA;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #4E5D94;
        }

        .password-toggle-icon {
            position: absolute;
            top: 50%;
            margin-top: 75px;
            right: 10px;
            margin-right: 5px;
            transform: translateY(-50%);
            cursor: pointer;
        }

        .password-toggle-icon i {
            font-size: 18px;
            line-height: 1;
            color: #fff;
            transition: color 0.3s ease-in-out;
            margin-bottom: 20px;
        }

        .password-toggle-icon i:hover {
            color: #fff;
        }
    </style>
</head>
<body>
    <div class="container">
        <img class="logo" src="https://cdn.itztiva.com/elixirlogo.png" alt="Logo">
        <h1>Welcome to Elixir! </h1>
        <br><br>
        <div class="auth-box">
            <h1>Create an Account</h1>
            <b>Press the Register button to join Elixir!</b>
            <form action="/elixir/registerdev" method="POST">
                <label for="username">Username:</label>
                <input id="username" name="username" value="<%= discordUsername %>" required>
                <label for="email">Email:</label>
                <input id="email" name="email" required>
                <label for="password">Password:</label>
                <input id="password" name="password" required>
                <input type="hidden" id="discordId" name="discordId" value="<%= id %>">
                <span class="password-toggle-icon"><i class="fas fa-eye"></i></span>
                <button type="submit">Register</button>
            </form>
        </div>
    </div>
    <script> document.addEventListener("DOMContentLoaded", function() {
        const passwordInput = document.getElementById("password");
        const toggleIcon = document.querySelector(".password-toggle-icon i");
    
        toggleIcon.addEventListener("click", function() {
            if (passwordInput.type === "password") {
                passwordInput.type = "text";
                toggleIcon.classList.remove("fa-eye");
                toggleIcon.classList.add("fa-eye-slash");
            } else {
                passwordInput.type = "password";
                toggleIcon.classList.remove("fa-eye-slash");
                toggleIcon.classList.add("fa-eye");
            }
        });
    });
    </script>
</body>
</html>
